@model IEnumerable<WebApplication1.Data.Models.Specialty>

<h2>Specialties</h2>
<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var specialty in Model)
        {
            <tr>
                <td>@specialty.Name</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@specialty.Id" class="btn btn-primary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@specialty.Id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-action="Create" class="btn btn-success">Add New Specialty</a>
